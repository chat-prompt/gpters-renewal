# UX 이슈 트래커

페이지별로 발견한 UX 문제를 기록합니다.

## 작성 방법

```
### 페이지명 (`/경로`)
- [ ] 이슈 설명 — 심각도(상/중/하)
```

---

## 홈 (`/`)


## 탐색 (`/explore/feed`)

- [x] 게시글 앞 숫자+화살표(투표 UI)가 직관적이지 않음 — 중 → PostCard에서 투표 UI 제거, 하트 아이콘(좋아요)으로 교체 (2026-03-04)
- [x] 게시글이 "글"처럼 느껴지지 않음. 본문 미리보기(프리뷰 텍스트)가 노출되어야 함 — 상 → excerpt line-clamp-3 노출, Medium 스타일 리스트형 (2026-03-05)
- [x] 블록형 카드 레이아웃이 답답함. 요즘IT/Medium/Branch처럼 리스트형으로 가독성 개선 필요 — 상 → Medium 스타일 리스트형, 보더 제거 → divide-y 디바이더만 사용 (2026-03-05)
- [x] 정렬 기준(인기/최신/추천)이 탭으로 되어 있는데, 우측 드롭다운이 더 깔끔할 듯 — 중 → ghost 스타일 드롭다운, 게시글 목록 위 좌측 정렬 (2026-03-05)
- [x] 태그 필터가 껍데기만 있음. 선택된 태그만 표시하는 UI인데 태그를 선택하는 방법(드롭다운/검색)이 없어서 작동 불가 — 상 → 전체 태그 토글 pill UI로 교체 (2026-03-04)
- [x] 카테고리 필터 선택해도 게시글이 필터링되지 않음. 프로토타입이라도 mock 필터링 로직 필요 — 상 → mock 필터링 로직 구현 (카테고리/태그/정렬) (2026-03-04)
- [x] 정렬 옵션 "인기"와 "추천"의 차이가 불명확. 인기/최신 2개로 충분 — 중 → 인기/최신 2개로 축소 (2026-03-04)
- [x] 카테고리에 디자인(Midjourney, DALL-E, Figma AI 등) 영역이 없음. AI 활용법에 묻힘 — 상 → 디자인 카테고리 추가 (2026-03-04)
- [x] 카테고리에 미디어/크리에이티브(영상, 이미지, 음악 — Runway, Suno, Pika 등) 영역이 없음 — 상 → 미디어 카테고리 추가 (2026-03-04)
- [ ] "AI 활용법" 카테고리명이 너무 포괄적/치우침. 다른 카테고리도 결국 AI 활용법인데 구분이 모호 — 중
- [x] "AI 뉴스"보다 "트렌드"가 더 적합할 수 있음. 뉴스는 단순 전달, 트렌드는 흐름+인사이트 포함 — 하 → 뉴스→트렌드 변경 완료 (2026-03-04)
- [ ] Badge/Tag 코드가 디자인 라이브러리와 불일치. 코드는 둘 다 `rounded-sm`이지만 디자인은 Badge(각진) / Tag(pill)로 분리됨 — 중


## 커뮤니티 (`/community/feed`)


## 게시글 상세 (`/posts/[slug]`)


## 스터디 상세 (`/study/[slug]`)


## 스터디 학습 (`/study/[slug]/learn`)


## 내 스터디 (`/study/my`)

- [x] GNB에서 내 스터디 1클릭 진입 불가 — 상 → GNB 중앙 네비에 내 스터디 버튼 추가 (2026-03-09)
- [x] 학습 페이지 간 이동 경로 없음 (사이드바 부재) — 상 → layout.tsx로 사이드바 5개 메뉴 도입 (2026-03-09)
- [x] 8개 섹션 수직 나열로 핵심 정보 파악 어려움 — 중 → 2x2 대시보드 그리드로 리라이트 (2026-03-09)
- [x] 게시글 모아보기 페이지 없음 — 중 → /study/my/posts 신규 구현 (2-panel + 필터) (2026-03-09)
- [x] 결제 완료 후 온보딩 안내 없음 — 중 → /checkout/complete 온보딩 페이지 신규 구현 (2026-03-09)


## 프로필 (`/profile/[username]`)


## 검색 (`/search`)


## 메시지 (`/messages`)


## 결제 (`/checkout/[studyId]`)


## 글쓰기 (`/write`)


## 어드민 (`/admin/*`)

- [x] 게시글 검색 입력해도 필터링 안 됨 — 상 → `searchQuery` state + `filteredPosts` 로직 구현 (2026-03-10)
- [x] 게시글 일괄 삭제/이동 버튼 동작 안 함 — 상 → `handleBulkDelete/handleBulkMove` 핸들러 연결, 이동 시 Select UI 토글 (2026-03-10)
- [x] 회원 역할이 Badge(읽기 전용)로만 표시 — 상 → Select 인라인 드롭다운으로 교체, `onValueChange`로 즉시 반영 (2026-03-10)
- [x] 회원 계정 정지 버튼 동작 안 함 — 상 → `suspended` 필드 + `toggleSuspend` 핸들러 + 행 opacity 표시 (2026-03-10)
- [x] 신고 처리 버튼(확인/무시/삭제) 동작 안 함 — 상 → `handleConfirm/handleIgnore/handleDelete` 핸들러 연결 (2026-03-10)
- [x] 신고 탭 건수가 정적으로 고정됨 — 중 → `pendingCount` 동적 계산으로 탭 레이블 갱신 (2026-03-10)
- [x] 배너 수정/삭제 버튼 동작 안 함 — 상 → 인라인 편집 패턴(editingId/editForm), Trash2 삭제 핸들러 (2026-03-10)
- [x] 배너 추가 기능 없음 — 상 → "새 배너" → 하단 addForm 인라인 추가 (2026-03-10)

## 기타

- [x] 페이지별 max-width가 혼용되었음 (7xl/6xl) — 중 → max-w-[1080px]로 22개 파일 통일 완료, 단 글쓰기/체크아웃은 의도적 예외 (2026-03-06)

